import type { ConfirmDialogProps } from './types'
import { ActionConfirmDialog } from '../../components/common/ActionConfirmDialog'

export function ConfirmDialog(props: ConfirmDialogProps) {
  return <ActionConfirmDialog {...props} />
}
